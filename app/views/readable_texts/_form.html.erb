<%= form_with(model: readable_text) do |form| %>
  <% if readable_text.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(readable_text.errors.count, "error") %> prohibited this readable_text from being saved:</h2>

      <ul>
        <% readable_text.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>

    <%= form.radio_button :language, "EN" %>
    <%= form.label :language_en, "English" %>
    <%= form.radio_button :language, "ZH" %>
    <%= form.label :language_en, "Chinese" %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.textarea :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
